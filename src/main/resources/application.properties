# API Configuration
# Provide empty values to prevent errors
api.base-url=
api.key=

# Knowledge Configuration
knowledge.fallback.response=No answer available

# Database Configuration
spring.datasource.url=jdbc:h2:mem:knowledgebase
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

# Set the server port to 8080 or use the PORT environment variable provided by Render
server.port=${PORT:8080}

# Other application properties
spring.datasource.url=jdbc:h2:mem:knowledgebase
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true

# Logging
logging.level.com.knowledgebase=DEBUG
logging.level.com.yourpackage=DEBUG